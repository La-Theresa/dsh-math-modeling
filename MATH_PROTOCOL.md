# 数学建模工作协议 (Mathematical Modeling Protocol)

本协议是 `math-modeling` preset 的默认方法论。开始任何数学建模任务前，请先阅读本文件，并在整个工作过程中遵守。

## 0. 环境与工作目录

**动机**：计算环境不稳定会直接中断建模流程；所有中间产物必须可恢复、可复现。

- 运行环境准备：
  ```bash
  bash math-modeling/setup-workbench.sh
  ```
- 所有计算优先使用 `math-modeling/.venv/bin/python`，不要依赖系统 `python3`。
- 禁止把虚拟环境、中间数据、日志放在 `/tmp`；`/tmp` 可能随 shell 超时或重启被清理。
- 项目内固定目录：
  ```text
  math-modeling/
    artifacts/   # 中间 npy/csv/json
    results/     # problem*.json / summary
    logs/        # 长任务日志
  ```
- 如果 `math_code` 报环境缺失，先执行环境自检，不要手工乱建 venv：
  ```bash
  math-modeling/.venv/bin/python -c "import numpy, scipy, sympy, matplotlib, pandas, openpyxl, pypdf, pdfplumber, statsmodels, sklearn"
  ```

## 1. 问题理解与目标

**动机**：建模不是直接写方程，而是先明确“要回答什么问题、精度要求是什么、可用的数据/约束是什么”。

- 写出问题背景、决策变量、已知数据、待求结果。
- 定义成功标准：预测精度、解释能力、稳定性、计算成本等。
- 明确边界：哪些因素必须纳入，哪些可以忽略并说明原因。

## 2. 假设与符号定义

**动机**：假设决定模型适用范围；统一符号避免推导混乱。

- 列出所有假设，并标明强假设/可放松假设。
- 建立符号表：变量、参数、单位、维度。
- 做量纲分析 (Dimensional Analysis) 和无量纲化 (Nondimensionalization)。

## 3. 从第一性原理建立方程

**动机**：机理模型应有可解释的因果链，而不是黑箱拟合。

- 守恒律：质量、动量、能量、物质/资金守恒。
- 动力学：牛顿定律、化学反应速率、种群增长、电路定律等。
- 均衡/优化：目标函数 + 约束，并说明凸性/可解性。
- 随机性：随机过程、期望/方差、置信区间。
- 数据驱动：仅当机理过复杂时使用，并说明理由 + 交叉验证。

## 4. 定解条件不完备与边界条件反演

**动机**：很多竞赛题不会给全边界条件。直接套 Dirichlet/Neumann 会系统性改变结果。

- 若换热系数、接触热阻、边界通量未知，不要默认 Dirichlet。
- 建立候选边界模型族，例如：
  - Dirichlet（可视为 $h\to\infty$）
  - Robin：$-k\\frac{\\partial T}{\\partial n}=h(T-T_{\\infty})$
  - Robin + 集总热容 / 多层接触热阻
- 用数据做嵌套模型选择 (Nested Model Selection)：
  - RMSE、AIC/BIC、残差结构、参数置信区间；
  - 优先估计未知边界参数，并报告不确定度。
- 明确说明“不同边界假设会如何影响后续优化结果”，例如厚度可能偏差 1~2 mm。

## 5. 模型求解：解析/符号优先，数值随后

**动机**：解析解提供全局结构；数值解处理实际复杂度。两者互补。

- 先用 `sympy` 尝试化简、求导、积分、求通解/特解、变换、特征值分析。
- 若解析不可行，用 `numpy/scipy` 设计数值方法。
- 数值求解前先判定问题类型：IVP、BVP、特征值问题、优化问题等。

## 6. 微分方程与差分格式

**动机**：离散化必须保证收敛、稳定、守恒，否则数值结果不可信。

### 6.1 显式 vs 隐式

- 显式格式：实现简单，但可能有严格稳定性限制。
- 隐式格式：每步需解方程/线性系统，但稳定性更好，适合刚性问题和长时间演化。
- 常用：Euler、改进 Euler、Runge-Kutta、Crank-Nicolson、向后 Euler、有限差分/有限体积/有限元。

### 6.2 每一步必须分析

- 一致性 (Consistency)：局部截断误差是否随步长趋于 0？
- 稳定性 (Stability)：Von Neumann / A-stability / L-stability。
- 收敛性 (Convergence)：一致性 + 稳定性 → 收敛性；给出数值阶验证。
- 守恒/单调性：是否保持质量、能量、非负性。

### 6.3 网格与层界面联动

- 若厚度等几何参数是优化变量，禁止使用单一全局均匀网格导致界面落在单元内部。
- 优先“逐层布点、每层局部均匀”，保证层界面始终是节点。
- 报告 `max local dx`，而不是只报告单一 `dx`。

## 7. 求解器准用测试 (Solver Acceptance Gate)

**动机**：求解器 bug 一旦进入反演/优化，错误会被放大且难以定位。必须先证明正问题求解器可信，才能用于参数估计或优化。

在进入反演/优化前，必须通过以下测试：

1. 稳态多层平板/简单几何解析解对照；
2. 退化情形（如 $T_{\\text{env}}=37$ 或均匀初始条件）验证；
3. 能量守恒相对误差；
4. 空间/时间收敛阶；
5. 显式与隐式在小步长下结果一致。

未通过准用测试的求解器，不得用于后续反演或优化。

## 8. 参数估计与模型选择

**动机**：模型参数必须由数据标定，并报告不确定度。

- 使用最小二乘、极大似然、贝叶斯估计等方法。
- 评估拟合优度：RMSE、R²、残差分析、AIC/BIC、交叉验证。
- 说明参数可辨识性 (Identifiability) 和共线性风险。
- 若存在多个候选模型，必须做模型选择并说明选择依据。

## 9. 多目标优化

**动机**：很多赛题要求同时优化多个目标，不能只靠枚举后人工挑点。

- 明确目标向量、约束、决策变量。
- 目标归一化，消除量纲影响。
- Pareto 支配定义：
  - 解 $x$ 支配 $y$：$x$ 所有目标不劣于 $y$，且至少一个目标严格更优。
- 常用方法：
  - 加权和 (Weighted Sum)
  - ε-约束法 (ε-Constraint)
  - NSGA-II / 差分进化等进化算法
- 识别退化 Pareto 前沿：若可行边界上所有目标同向变化，Pareto 集可能退化为单点，必须用支配关系证明。
- 制造/离散精度下，结果需要“向上圆整”并回代验证可行性。

## 10. 仿真驱动优化 (Simulation-Based Optimization)

**动机**：PDE 约束优化不是“解一次 PDE”，而是正问题求解器反复嵌入优化循环。

标准流程：

1. 先通过第 7 节求解器准用测试；
2. 用单调性/最大值原理化简约束，减少不必要仿真；
3. 粗网格扫描定位可行区间；
4. 二分/黄金分割/贝叶斯优化精修；
5. 最优解回代最细网格验证；
6. 对数值参数做 Richardson 外推，报告连续解与圆整解。

## 11. 数值约束容差

**动机**：浮点误差会把恰好在边界上的最优解误判为不可行。

- 所有数值约束比较必须带显式容差，例如：
  ```python
  feasible = (T_end <= 47 + 1e-6) and (T_cross <= 44 + 1e-6)
  ```
- 报告内容必须包含：
  - 约束临界点数值；
  - 所用容差；
  - 阈值穿越时刻。

## 12. 单位与输入合法性自检

**动机**：mm/m 混淆或量级错误会生成超大网格，导致超时和无效结果。

- 代码入口统一单位：长度用 m、温度用 °C、时间用 s。
- 加入物理范围 assert，例如：
  ```python
  assert 0.0006 <= d <= 0.025
  ```
- 扫描/大规模计算前先打印：
  ```text
  (d2_mm, N, dx, dt, steps)
  ```
- 若网格节点数或步数异常大，先停下来检查单位与几何。

## 13. 灵敏度与稳健性分析

**动机**：结论不能只依赖一组参数。

- 对关键参数做单变量/多变量灵敏度分析。
- 检查模型对假设扰动的响应。
- 需要时做不确定性传播 (Monte Carlo / polynomial chaos)。

## 14. 验证、误差分析与结果解释

**动机**：模型只有通过验证才有说服力。

- 用解析解、退化情形、守恒量、实验/历史数据验证。
- 给出误差随网格/步长变化的收敛阶。
- 将结果翻译为对原始问题的结论，说明适用条件和局限。

## 15. 可视化、中间产物与交付

**动机**：图和可复现文件是沟通推导与结果的最佳载体。

- 用 `matplotlib` 绘制数据、模型曲线、误差、灵敏度、相图、稳定性区域。
- 图片保存后，用程序化方式确认：
  ```python
  import os
  assert os.path.exists(path) and os.path.getsize(path) > 0
  ```
  **视觉复查是可选项**，不能作为唯一验证手段。
- 每阶段结束后，可用以下脚本把 markdown/代码追加到 notebook：
  ```bash
  math-modeling/.venv/bin/python math-modeling/write_notebook.py math-modeling/workbench.ipynb --markdown "## 阶段记录" --code "print('done')"
  ```
- 每个问题结束时生成：
  ```text
  results/problem1.json
  results/problem2.json
  results/problem3.json
  SUMMARY.md
  run_all.sh
  ```
- 在 `results/*.json` 中记录版本与数据指纹：
  - 数据文件 SHA-256；
  - Python / numpy / scipy 版本；
  - 网格和时间步长；
  - 随机种子（若有）；
  - 关键文件路径。
- Excel 输出建议：
  - 使用 `openpyxl` 的 `write_only=True` 写大矩阵；
  - 写后只读抽查首/中/末行和 sheet 维度；
  - 大文件可提示压缩或改为长表格式。

## 16. 每一步的自检清单

- [ ] 我是否运行了 `bash math-modeling/setup-workbench.sh` 并确认环境可用？
- [ ] 我是否从第一性原理说明了模型来源？
- [ ] 我是否列出了所有假设与适用范围？
- [ ] 我是否给出了完整推导而不是只给结果？
- [ ] 我是否比较了至少两种建模角度？
- [ ] 定解条件不完备时，我是否做了边界条件反演/模型选择？
- [ ] 若涉及微分方程，我是否讨论了显式/隐式、稳定性、收敛性？
- [ ] 求解器是否通过了准用测试？
- [ ] 多目标问题是否做了 Pareto 分析，而不是枚举后人工挑点？
- [ ] 数值约束是否带容差？
- [ ] 单位与物理范围是否经过自检？
- [ ] 我是否用 `math_code` 或 notebook 保存了可复现计算？
- [ ] 我是否做了灵敏度/验证/误差分析？
- [ ] 我是否在 `results/` 中保存了版本指纹和关键结果？
- [ ] 我是否给出了清晰的结论与可视化？
